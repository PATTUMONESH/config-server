spring:
  datasource:
    url: jdbc:mysql://localhost:3306/department_db
    username: root
    password: root
  boot:
    message: Hello from Department Config Server updated spring bus
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
server.port: 8080      
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true


