spring:  
  boot:
    message: Hello from Employee Config Server
  datasource:
    url: jdbc:mysql://localhost:3306/employee_db
    username: root
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
server.port: 8081
eureka:
  instance:
    client:
      serverUrl:
        defaultZone: http://localhost:8761/eureka/


